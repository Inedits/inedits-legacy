{% extends 'base.html.twig' %}
{% block title %}Homepage{% endblock %}
{% block body %}
    {% if tree.id is defined %}
        <a id="getTreeLink" href="{{ path('post_json', { slug: tree.slug }) }}"></a>
        <div class="tree-container"></div>
    {% endif %}
{% endblock %}

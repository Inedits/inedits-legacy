{% if path is not null %}
    <img class="{{ class|default }}" src="{{ asset(path, 'avatar') | imagine_filter(config) }}" alt="{{ alt }}" />
{% else %}
    <img class="{{ class|default }}" src="{{ asset('avatar.jpg', 'default') | imagine_filter(config) }}" alt="{{ alt }}" />
{% endif %}

{% if avatar_path is not null %}
    <img class="img-circle" src="{{ asset(avatar_path, 'avatar') }}">
{% else %}
    <img class="img-circle" src="{{ asset('avatar.jpg', 'default') }}" width="100" height="100">
{% endif %}

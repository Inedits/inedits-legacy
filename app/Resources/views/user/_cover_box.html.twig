{% if path is not null %}
    <img class="img-responsive" src="{{ asset(path, 'cover') }} | imagine_filter('user_box') }}" />
{% else %}
    <img class="img-responsive" src="{{ asset('cover.jpg', 'default') | imagine_filter('user_box') }}" />
{% endif %}

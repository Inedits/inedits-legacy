{% if avatar_path is not null %}
    <img class="img-responsive" src="{{ asset(avatar_path, 'avatar') }}">
{% else %}
    <img class="img-responsive" src="{{ asset('avatar.jpg', 'default') }}">
{% endif %}
